# Prisma Database Connection
# Note: Prisma requires the full connection string here. It must match your Docker Postgres variables below.
DATABASE_URL="postgresql://user:password@localhost:5432/db_name?schema=public"

# NextAuth Configuration
NEXTAUTH_SECRET="generate-a-secure-secret-key-here"
AUTH_URL="https://your-production-domain.com" # Required in production (or NEXTAUTH_URL for older versions)
AUTH_TRUST_HOST="true" # Required when hosting behind a reverse proxy (Nginx, PM2, etc.)

# Gemini AI Key
GOOGLE_GENERATIVE_AI_API_KEY="your-gemini-api-key-here"

# Docker Postgres Variables (Used by docker-compose to initialize the DB container)
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=db_name
DB_PORT=5432

NEXT_PUBLIC_APP_URL=